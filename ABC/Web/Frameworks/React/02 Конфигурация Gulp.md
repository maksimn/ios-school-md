Файл называется 

```js
/// gulpfile.js
"use strict";

var gulp = require('gulp');

// Для работы локального дев-сервера:
var connect = require('gulp-connect'); 

// Для того, чтобы открыть URL в веб-браузере:
var open = require('gulp-open');
```